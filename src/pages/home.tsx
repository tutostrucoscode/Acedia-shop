import Alert from "../features/ui/Alerts";
import TemplateGeneral from "../features/ui/TemplateGeneral";

const Home = () => {
  return (
    <TemplateGeneral>
      <Alert />
    </TemplateGeneral>
  );
};

export default Home;
